<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Дополнительные стили для компонентов -->
    <style name="AppButton" parent="Widget.Material3.Button">
        <item name="android:textColor">#ffffff</item>
        <item name="backgroundTint">@color/md_theme_primary</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <style name="AppSearchView" parent="Widget.Material3.SearchBar">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textColorHint">@color/md_theme_onSurfaceVariant</item>
        <item name="android:backgroundTint">@color/md_theme_surfaceContainerHigh</item>
    </style>
</resources>